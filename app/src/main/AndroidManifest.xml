<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Galabouttest"
        tools:targetApi="31">
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".CreateAccountActivity"/>
        <activity android:name=".ui.account.UpdateProfileActivity"/>
        <activity android:name=".ui.home.HomeFragment"/>
        <activity android:name=".ui.home.HomeViewModel"/>
        <activity android:name=".ui.accommodations.AccommodationFragment"/>
        <activity android:name=".ui.accommodations.AccommodationViewModel"/>
    </application>

</manifest>